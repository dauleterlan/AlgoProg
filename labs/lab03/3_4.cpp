#include <iostream>
#include <cmath>
using namespace std;
int main() {
    double x , y
    cin >> x >> y >>
    double A = 2*cos*(x-M_PI/6);
    double B = 1/2 + (pow(sin(y),2));
    double resA = A/B;
    double A2 = atan(1/z);
    double resB = cos(A2) * cos(A2);
    cout << resA << " " << resB;
    return 0;
    c
}   
